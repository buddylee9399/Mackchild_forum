#post_content
	%h1= @post.title
	%p= @post.content

	#comments
		%h2
			= @comments.count
			Comments
		= render @comments

		%h3 Reply to thread
		= render "comments/form"

	%br/
	%hr/
	%br/

	= link_to "Edit", edit_post_path(@post), class: "button"
	= button_to "Delete", post_path(@post), method: :delete, data: { confirm: "Are you sure you want to do this?" }